; Zone file for {{ zone.name }}
; Generated by SpatiumDDI — do not edit by hand.
$TTL {{ zone.ttl }}
{{ zone.name }} IN SOA {{ zone.primary_ns }} {{ zone.admin_email }} (
    {{ zone.serial }} ; serial
    {{ zone.refresh }} ; refresh
    {{ zone.retry }} ; retry
    {{ zone.expire }} ; expire
    {{ zone.minimum }} ; minimum
)
{% for r in records -%}
{{ render_record(zone, r) }}
{% endfor -%}
