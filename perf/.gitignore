# Live run state + artifacts are regenerated per run — never committed.
# (Rendered reports under perf/reports/<run_id>/ ARE committed for reproducibility.)
run/
*.pyc
__pycache__/
.venv/
venv/
*.egg-info/
.pytest_cache/
*.hdr
*.tsdb
